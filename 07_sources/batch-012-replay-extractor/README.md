# Batch 012回放摘要复现

用途：仅用于Match 6227492909的取证。不是小说运行依赖，不自动联网、不改main、不产生外部写入。

来源与哈希见[来源记录](../batch-012-ti10-evidence.md)。使用Go 1.27.1、Manta v1.5.0和Python 3标准库。本目录go.mod/go.sum限定本次隔离提取工具；不要求给小说仓库安装长期服务。

在本目录的临时副本操作：

1. 从来源记录的Valve URL取得压缩回放，确认124472677字节和SHA256，再用bzip2解压为`6227492909.dem`。
2. `go run . 6227492909.dem`，标准输出保存为UTF-8的`snapshots.jsonl`；程序另外写`6227492909.dem.fileinfo.json`。
3. 在相同临时目录运行`python -X utf8 summarize.py`，产生`selected-snapshots.json`与`publication-extract.json`。
4. 将publication-extract的内容与仓库`../batch-012-ti10-extract.json`比较。目标时刻相同，原始网络浮点值不做插值或四舍五入替换；Markdown展示值可以注明近似。

重要校验：回放内部ID、构建号5061；完整解析至tick93145；39个目标时刻，每次10个真实选手英雄；9:00天怒4级、1236经验、820生命、约602.94魔法、加点1/1/2/0。必须通过玩家选中英雄句柄匹配实体index和serial，不能按英雄类名取第一个实体，后者会把复制体混入。

库存槽位和可见性原始标记不等于完整玩家视角。脚本不判断谁注意到敌人、不复原队内语音、不生成战术最优解。Manta包中可能存在已经删除的空实体，已在筛选时排除。

请勿将压缩回放、解压回放、完整原始实体输出或便携运行时加入小说仓库。这里只版本管理原创提取方法、依赖锁定和少量派生事实。
